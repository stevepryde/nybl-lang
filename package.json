{
  "name": "nybl-lang-site",
  "private": true,
  "scripts": {
    "release:check-licenses": "bun run scripts/check-package-licenses.ts",
    "site:llms": "bun run scripts/llm-docs.ts",
    "site:assets": "bun run site:llms && bun run scripts/site-assets.ts",
    "site:css": "bun run site:assets && tailwindcss -i docs/styles/site.css -o docs/static/site.css --minify",
    "site:check": "bun run site:css && zola --root docs check",
    "site:build": "bun run site:css && zola --root docs build",
    "site:serve": "bun run site:css && zola --root docs serve",
    "site:deploy": "bun run site:build && wrangler pages deploy docs/public --project-name=nybl-docs --branch=main"
  },
  "devDependencies": {
    "@fontsource-variable/jetbrains-mono": "5.3.0",
    "@fontsource-variable/source-sans-3": "5.3.0",
    "@tailwindcss/cli": "^4.3.3",
    "tailwindcss": "^4.3.3",
    "wrangler": "^4.113.0"
  }
}
