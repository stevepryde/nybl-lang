[workspace]
members = [
    "nybl",
    "nybl-cli",
    "nybl-compile",
    "nybl-sys",
    "nybl-vm",
    "examples/rust-embedding",
]
resolver = "2"

[workspace.package]
version = "0.4.2"
edition = "2024"
# Rust 1.88 is the minimum toolchain for the complete 0.4 workspace,
# including the CLI dependency graph. Keep this in sync with the
# release check documented in CHANGELOG.md and specs/stack.md.
rust-version = "1.88"
license = "MIT OR Apache-2.0"
repository = "https://github.com/stevepryde/nybl-lang"
homepage = "https://github.com/stevepryde/nybl-lang"
authors = ["Steve Pryde"]
